{
	"buildCommand": "pnpm build",
	"framework": "vite",
	"installCommand": "rm -rf node_modules pnpm-lock.yaml ~/.pnpm-store ; pnpm install",
	"outputDirectory": "dist",
	"rewrites": [
		{
			"destination": "/server.js",
			"source": "/(.*)"
		}
	]
}
